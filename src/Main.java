import javax.swing.*;
import java.awt.*;
import GUI.MainFrame;

public class Main {
    public static void main(String[] args) {
        // Создаем объект MainFrame и вызываем метод для старта
        MainFrame mainFrame = new MainFrame();
        mainFrame.startMainFrame();
    }
}